# 🔒 Jobzee Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# ================================
# 🚨 CRITICAL SECURITY VARIABLES  
# ================================

# JWT Secret - MUST be a strong random string (minimum 64 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# WARNING: NEVER use the example value below in production!
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_64_CHARACTER_RANDOM_STRING_FOR_PRODUCTION

# Admin initialization key (production only)
# Only set this if you need to initialize admin accounts in production
ADMIN_INIT_KEY=your-super-secret-admin-init-key-for-production-only

# ================================
# 🗄️ DATABASE CONFIGURATION
# ================================

# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/jobzee
# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jobzee

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Application Settings
APP_NAME=JobZee

# ================================
# 📧 EMAIL CONFIGURATION
# ================================

# SMTP Settings (Required for password reset and notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email From Settings
FROM_EMAIL=noreply@yoursite.com
FROM_NAME="Jobzee Platform"

# Legacy email settings (for backward compatibility)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-or-smtp-password
EMAIL_FROM=noreply@jobzee.com

# ================================
# ☁️ CLOUDINARY (File Uploads)
# ================================

CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key  
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Example configurations for different email providers:

# Gmail Configuration (recommended for development)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=your-app-password (generate from Google Account settings)

# Outlook/Hotmail Configuration
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@outlook.com
# EMAIL_PASS=your-password

# Yahoo Configuration
# EMAIL_HOST=smtp.mail.yahoo.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@yahoo.com
# EMAIL_PASS=your-app-password

# SendGrid Configuration (production recommended)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASS=your-sendgrid-api-key

# Mailgun Configuration (production alternative)
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-mailgun-username
# EMAIL_PASS=your-mailgun-password

# AWS SES Configuration (enterprise)
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-ses-smtp-username
# EMAIL_PASS=your-ses-smtp-password

# Notes:
# - If EMAIL_* variables are not set, the forgot password will work but no email will be sent
# - For Gmail, use App Passwords instead of your regular password
# - For production, use services like SendGrid, Mailgun, or AWS SES
# - Set EMAIL_SECURE=true only for port 465 (SSL)
# - Set EMAIL_SECURE=false for ports 587/25 (TLS/STARTTLS)
